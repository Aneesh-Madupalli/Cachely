package com.cachely.app

import android.app.Application

/**
 * Application entry. Minimal global configuration only.
 * No business logic, permission logic, background work, or threading.
 */
class CachelyApplication : Application()
